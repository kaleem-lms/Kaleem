---
week: 2026-W24
phase: A
---

## Shipped this week

- **Phase A — identity module: Definition of Done closed.** Backend was already
  code-complete (78 tests, models 93% / services 96% coverage, ruff + mypy + import-linter
  green); this week walked the rest of the DoD.
- Ran the full golden-path smoke test against the live dev stack over real HTTP (CSRF +
  sessions + allauth verification): register parent → confirm email → login → `GET /me/` →
  add child → create invite, then register adult student → confirm → login → accept invite
  (`linked: true`). All green.
- Edge cases verified manually: login before verification → 400 "not verified"; invalid
  invite code → typed 400 (no bare-exception 500); authenticated mutation without CSRF →
  403. Anonymous register without CSRF returns 201 — confirmed this is standard DRF
  `SessionAuthentication` behaviour, not a regression.
- Wrote `docs/architecture/identity.md` (data model, public service API, auth/HTTP, v1
  decisions).
- **Staging deploy — done, and it shook out three latent infra problems.** (1) Backend
  and infra submodule commits were never pushed to their remotes (meta pinned local-only
  SHAs) → CI `not our ref` on checkout; landed via PRs. (2) Ruff formatter version skew —
  pre-commit pinned v0.6.0 while CI floated to 0.15.16 — failed `ruff format --check`;
  pinned both, and installed the backend pre-commit hooks (they were never installed).
  (3) First deploy failed on staging-DB `InconsistentMigrationHistory` (account before
  identity); recreated the empty staging DB and re-deployed green.
- **Email (ADR-0016).** A live staging smoke test found `register` → 500: production had
  no SMTP config. Added env-driven SMTP settings + AWS SES, deployed, and re-tested live:
  register → 201, login-before-verify → 400, unauth → 403. Phase A DoD fully closed.

## In progress

- Nothing — Phase A closed.

## Slipped or blocked

- Nothing blocked. Filed the frontend-deploy gap (dashboard/marketing not deployed) in
  ISSUES for a Phase-B deploy spec.

## Decisions made (link ADRs)

- **ADR-0016** — transactional email via AWS SES (SMTP). Reconfirmed ADR-0013 (admin UX
  in the React dashboard) and ADR-0015 (container baseline).

## Next week's focus

- Open Phase B (per roadmap): re-read the roadmap, write the next module's spec (D1).

## Energy check (1-5)

4

## Mid-week update (2026-06-13)

- Shipped the **frontend delivery pipeline** (ADR-0019): dashboard + marketing now build to
  GHCR nginx images and deploy behind Traefik (`app-staging`/`api-staging`/`staging`),
  same-site cookies + CORS. Deployed green after fixing a readiness-probe `ALLOWED_HOSTS`
  gap and the VPS `APP_DOMAIN`→`API_DOMAIN` rename. Mailpit wired for local dev. Closed the
  frontend-deploy-gap + mailpit items in ISSUES.
- Found the dashboard is still the Phase-0 scaffold (no auth UI) — Phase A identity shipped
  backend-only. **Next:** brainstorm the design system / themes / frontend+marketing
  identity, then build the identity frontend slice. (Fresh session to follow.)

## Design system shipped to staging (2026-06-13)

- Brainstormed → specced (D1) → planned (D2) → built subagent-driven (D3 TDD): the
  **Serene Scholar** design system. New **`@kaleem/tokens`** repo (5th submodule, v0.1.1):
  CSS vars on shadcn's contract + Tailwind v4 `@theme` preset, consumed by the React
  dashboard and Astro marketing — tokens shared, components diverge. Dashboard ships shadcn
  `src/ui/` primitives, light/dark theming, full RTL i18n (en/ar), lucide icons, and an
  a11y + AA-contrast(both themes) + RTL gate (23 tests). **ADR-0020** ratifies a11y/i18n/
  localization as a repo-wide baseline.
- **Latent bug fixed:** `index.html` pointed at the dead vanilla `main.ts`, so the
  "shipped" dashboard was serving the Vite counter demo, not React. Now `main.tsx`.
- **Animations** were scoped, offered, and **dropped** (user) — keeps the calm brand.
- Deployed to staging via `develop → master`. Getting CI green took four fixes:
  `TOKENS_REPO_TOKEN` scope for the private token dep; tokens submodule must use an https
  URL (SSH broke `actions/checkout`); **pnpm 9 → 10** (pnpm 9 + Vite 8/rolldown choked on
  the git-dep `node_modules` path); `GHCR_PAT` needs `write:packages`. Verified live:
  `app-staging` serves the real React app and `/design-preview` renders light/dark/RTL.
- **Next:** the identity frontend slice (login/register/verify/me) on this design system,
  proving the cross-subdomain session cookie end-to-end.

_Handoff 2026-06-13: design system shipped + verified live on staging; DoD closed. Next
session starts the identity frontend slice (D1 spec first) — see STATE.md "Next"._

## Dockerized dev environment shipped (2026-06-14)

- **`just dev`** brings the whole stack behind Traefik subdomains (`*.kaleem.localhost`),
  mirroring the ADR-0019 topology so same-site cookie/CSRF/CORS bugs surface in dev, not
  on staging. All six services verified 200 end-to-end; HMR works through the proxy;
  login sets a `Domain=.kaleem.localhost` cookie; 104 backend tests pass in-container.
- **Containers run as the host UID/GID** (injected by the justfile via `id -u`/`id -g`):
  no root-owned bind-mount files.
- **Two integration fixes discovered and resolved:**
  1. Traefik image upgraded to **v3.6** — v3.3 was incompatible with Docker Engine 29's
     minimum API version and refused to start.
  2. Frontend dev Dockerfiles install pnpm via **`npm i -g pnpm@10.28.0`** (not corepack)
     so pnpm is available to the non-root user without corepack's global-shim dance.
- Developer guide written: `docs/developer-guide/local-dev.md`.

_Handoff 2026-06-14: local Docker dev env shipped. Next: identity frontend slice (D1 spec first)._

## Identity frontend slice — Phase 1 (auth core) built (2026-06-14)

- Brainstormed → spec (D1, `2026-06-14-identity-frontend-design`) → plan (D2). Full identity
  surface, 3 phases (auth core → parent → student); `useMe`+`_authed` guard; custom
  `/verify-email` landing; role-aware home; availability grid deferred to Phase 3.
- **Phase 1 (auth core) built subagent-driven TDD and opened as PRs (not yet merged):**
  backend [#15](https://github.com/kaleem-lms/backend/pull/15) (`/verify-email/` confirm-by-key,
  adapter URL override, `FRONTEND_URL`; 99 tests green), dashboard
  [#6](https://github.com/kaleem-lms/dashboard/pull/6) (register/login/verify/me/home/profile,
  i18n+RTL+a11y; 47 tests green; removed placeholder `/` + `/design-preview`), meta docs
  [#75](https://github.com/kaleem-lms/Kaleem/pull/75).
- **Verified e2e on the local dockerized stack:** register → mailpit link → verify-email →
  login → `/me` all 200.
- **Two env gotchas** (now memories): dead pip proxy → commit with `PIP_CONFIG_FILE=/dev/null`
  (never `--no-verify`); a new dashboard dep needs an image rebuild + `node_modules` volume
  reseed (host `pnpm add` left the container's vite unable to resolve react-hook-form → the
  "dashboard doesn't work in docker" report; fixed). Also an **uncommitted** meta fix:
  `VITE_API_URL` must include `/api/v1` or the browser dashboard 404s every API call.
- **Next:** M1 — merge PRs, bump pointers, set `DJANGO_FRONTEND_URL` on the VPS, promote
  `develop → master`, then the real browser click-through on `app-staging` closes Phase A.

_Handoff 2026-06-14 (eve): identity auth-core built + PRs open; CI not yet checked. Resume at STATE.md "Next (immediate) — M1"._
