---
week: 2026-W24
phase: A
---

## Shipped this week

- **Phase A — identity module: Definition of Done closed.** Backend was already
  code-complete (78 tests, models 93% / services 96% coverage, ruff + mypy + import-linter
  green); this week walked the rest of the DoD.
- Ran the full golden-path smoke test against the live dev stack over real HTTP (CSRF +
  sessions + allauth verification): register parent → confirm email → login → `GET /me/` →
  add child → create invite, then register adult student → confirm → login → accept invite
  (`linked: true`). All green.
- Edge cases verified manually: login before verification → 400 "not verified"; invalid
  invite code → typed 400 (no bare-exception 500); authenticated mutation without CSRF →
  403. Anonymous register without CSRF returns 201 — confirmed this is standard DRF
  `SessionAuthentication` behaviour, not a regression.
- Wrote `docs/architecture/identity.md` (data model, public service API, auth/HTTP, v1
  decisions).

## In progress

- Staging deploy — the only remaining DoD item. CI auto-deploys on merge to `master`
  (`STAGING_HOST`/`STAGING_USER`/`STAGING_SSH_KEY` secrets); pending confirmation the VPS
  is provisioned and secrets are set. See STATE.md.

## Slipped or blocked

- Nothing blocked.

## Decisions made (link ADRs)

- No new ADRs this week. Reconfirmed existing decisions: admin UX in the React dashboard,
  not Django admin (ADR-0013); container baseline (ADR-0015).

## Next week's focus

- Finish the staging deploy + smoke-test it, then open Phase B.

## Energy check (1-5)

4
