---
week: 2026-36
phase: B — Billing (on staging, not closed)
---

## Shipped this week

- **Trunk-based flow adopted (ADR-0028); `develop` is deleted.** ADR-0014's `feat → dev →
  PR → master` had been false for four of five repos since the day it was written — the
  submodules never grew a `dev` branch. The layer survived only in meta, which is exactly
  where it did harm: squash-merged promotions meant the branches never shared history, every
  promotion needed a manual merge back to reconcile submodule pointers, and a squash once
  silently dropped a pointer bump. Its stated justification was pre-trunk verification that
  was not happening. Closed two `ISSUES.md` entries that each said "needs an ADR".
  - Sequenced deliberately: final `develop → master` promotion (#137, deploy green) →
    ADR PR straight to `master` (#138, the first under the new flow) → branch deleted, after
    verifying `develop` was an ancestor of `master` and zero commits would be lost.
  - The cost is real and is written into `CLAUDE.md` rather than buried: **a merge to the
    meta trunk is now a deploy.** That is only safe because the gates were made real first;
    adopting this three weeks ago, when CI did not run on `develop` at all, would have been
    reckless.
- **API versioning is enforced, not just chosen (ADR-0029, backend #37).** ADR-0017 settled
  the scheme in June but left it uncheckable and effectively scoped to the one module that
  existed. Nine are still unbuilt. A test now walks the *resolved* URLconf — not source
  text, so `include()`d routes count — and fails on anything outside `/api/v<n>/` or a
  closed three-entry allowlist. Verified to bite. Writing an ADR that promised a gate and
  not building the gate would have repeated the exact mistake this week was about.

- **The Playwright e2e harness exists** (ADR-0027) — the last wholly-unmet ADR-0021 clause,
  open since 2026-06-17. Spec → plan → seed → harness → 6 identity specs → CI job, green in
  CI at 2m6s, and `deploy-staging` now depends on it.
  - **Design decision that carries the whole thing:** CI serves `app.kaleem.localhost:4173`
    and `api.kaleem.localhost:8000`, not two ports on `localhost`. **Cookies ignore ports**,
    so the `localhost` arrangement would share a session by accident, pass happily, and
    prove nothing about the ADR-0019 cross-subdomain cookie — which is the one thing this
    suite can prove that vitest cannot, and which has broken before.
  - **D4 caught a design error before any code.** The first draft put the seed in
    `kaleem.platform`; `import-linter`'s "platform imports no business modules" is an
    *independence* contract, so it could never call `identity.services`. Which also means
    the long-standing `platform/seed.py` stub is not merely unfinished — it is
    architecturally impossible where it sits.
  - **Mutation-checked, not merely run.** Pointing the login-gate spec at a *verified*
    account, and asserting the anonymous visitor is *not* redirected, both go red. A green
    e2e suite that would stay green if the app broke is worse than none.
  - Scoped to identity and `CLAUDE.md` says so in a table. Three of the six flows cover bugs
    this codebase actually shipped: the login gate that checked the wrong address, the
    verify-email infinite spinner (regressed twice), and the `_authed` guard.
- **Two follow-ups from verification, both fixed** (backend #35, dashboard #30): the
  `unknown_subscription` incident fired on *every* checkout (4 of 4) and is now `info`, with
  `invoice.payment_failed` deliberately left a warning; and a 403 eligibility rejection said
  "please try again", advice that could never work.

- **Webhook endpoint rotated onto an explicitly pinned API version.** Old
  `2025-06-30.basil` endpoint deleted; new one created at `2026-08-26.dahlia`, signing secret
  rotated on the VPS, `DJANGO_STRIPE_API_VERSION` now set explicitly. Verified with a live
  subscription: events delivered and signature-verified against the new secret, renewal date
  rendered immediately.
  - **The reason the plan changed mid-task is worth recording.** While writing the
    step-by-step guide I re-read the SDK's pin and it had moved — `2026-07-29.dahlia` →
    `2026-08-26.dahlia` — **between two deploys on the same day**, because
    `requirements/base.txt` allows `stripe>=15.5,<16.0` and the rebuild picked up 15.6.1. So
    "pin the endpoint to whatever the SDK says" was a treadmill, not a fix. Pinning *both*
    sides via `DJANGO_STRIPE_API_VERSION` is the actual fix. The runbook said the opposite
    ("leave it unset, the default is read off the installed SDK") and has been corrected.
  - Reminder for production: `api_version` is **create-only** on a webhook endpoint. Cheap to
    get right at creation, a secret rotation afterwards.
- **The D3 coverage gate is real for the first time**, in all the repos that have tests.
  - `dashboard` #29 — `@vitest/coverage-v8` was **not installed**, so coverage had never
    been measured here at all. Installed it (pinned to the vitest 3.x line; the default
    resolution pulls v4, which needs vitest 4), scoped it in `vitest.config.ts` with
    `all: true` so an untested module reads 0% rather than being invisible, and set
    thresholds to the measured baseline: **92.88% lines / 87.80% branches / 84.40%
    functions** → floors 92/87/84.
  - `backend` #33 — CI had run `--cov-branch` since July but never `--cov-fail-under`.
    Floor set in `pyproject.toml` at **97** (measured 97.26%), `branch = true` moved into
    the run config so branch measurement is a property of the repo rather than of the
    command line.
  - meta — `ci.yml` now runs `pnpm lint` and `pnpm test:coverage` for the dashboard. Until
    this week the dashboard job ran **`tsc` only**: vitest, biome and coverage had never
    executed in CI.
- **Fixed the renewal-date race the click-through found, and verified the fix under the
  conditions that caused it.** Spec (D1) → 7 failing tests (D3) → fix → backend #34 → meta
  #130 → promotion #131 → `deploy-staging` green. On the post-deploy re-test the race
  *reproduced exactly* — `invoice.paid` arrived first at 16:08:25.348 and was dropped again —
  and the card showed "Renews on October 3, 2026" immediately anyway. That is a better
  verification than the race not happening would have been.
- **The ratchet floor earned itself within a week.** My first pass at the fix took the repo
  from 97.26% to **96.96%** and CI went red: I had added `get_subscription` to the Stripe
  provider without covering it. Seven days after ADR-0026 was written up as a retreat from
  the 100% standard, the smaller number everybody enforces caught a real regression that the
  bigger number nobody enforced would not have.
- **Declined-card leg done:** `4000 0000 0000 0341` → 0 subscriptions, not entitled, no
  incidents. Stripe confirms the first payment inline, so a bad card writes nothing.
- Resolved the stale `webhook_silence` critical incident (80 occurrences) now that delivery
  has resumed — runbook §7.

- **Billing click-through PASSED on staging — the 17-day blocker is cleared.** Verified the
  two Stripe-console configs against the Stripe **API** rather than the console UI, which was
  the right call: the first check found only 6 of 8 event types subscribed and the portal's
  subscription-update switched off. After two rounds of fixes (the portal one applied via
  `billing_portal.Configuration.modify`, since console saves were not taking), ran the full
  flow with Playwright: subscribe → hosted checkout `4242…` → subscription card → portal →
  cancel. kaleem and Stripe agree exactly: `active`, `cancel_at_period_end=True`, period end
  `2026-10-03 04:18:39` both sides. Idempotency held — webhook *and* `settle:` both ran, one
  row. Webhooks are flowing again (first delivery since 2026-08-13).
  - **A caution worth keeping:** `billing_portal.Configuration` reports
    `subscription_update.products = null` at every API version even when plan switching
    demonstrably works. I initially read that as "nothing switchable" and reported it as a
    fault; the portal itself showed both plans. Test the portal, don't trust the field.

## The defect the click-through found

`invoice.paid` arrived **80ms before** `checkout.session.completed`:

```
04:18:43.553  invoice.paid               <- carries the period end
04:18:43.561  unknown_subscription       <- no row yet, event DROPPED
04:18:43.633  checkout.session.completed <- creates the row, no period end
04:18:46.281  settle:cs_test_...         <- idempotent, no duplicate
```

A Checkout Session payload has no `current_period_end` and no `items`, so the row is created
with `None` and the UI shows "Active" with no renewal date. Only the nightly reconciler fixes
it — the `reconciled_drift` incidents for users 5 and 7 are exactly this, so it is
reproducible, not a fluke. Logged in `ISSUES.md` rather than fixed on the spot (D10); it needs
its own D1 slice.

This is the argument for click-throughs in one example: 479 backend tests and 363 dashboard
tests are green, `billing` is at 100% line+branch, and none of them caught it — because the
race only exists between two real webhook deliveries.

- **CI now runs on `develop` at all.** Found while waiting for this week's own meta PR to go
  green: `ci.yml`'s triggers were `[master, main]`, so **no `feat/… → develop` PR has ever
  run CI in the meta repo**. The first time anything was checked was the `develop → master`
  promotion — which is also the deploy. That is the mechanism behind "first green CI for
  Phase B" only happening at #126, three weeks after the code was written. Checks belong on
  the PR that introduces a change, not on the one that ships it. `deploy-staging` is
  unaffected (still gated on `push` to `master`).
- **`STATE.md` cut 517 → 57 lines.** It had become an append-only session log. History moved
  verbatim to `journal/session-log-2026-06-to-08.md`; the state file is now current position,
  next actions and in-flight work, with a stated line budget.
- **`ISSUES.md` retriaged by consequence** (blocks launch / blocks a phase close / someday),
  replacing the by-date ordering. **11 entries deleted as already-fixed or stale** — verified
  individually, not assumed: `mypy kaleem` is clean (the `platform/drf.py:28` entry),
  `LIVE_STATUSES` now includes `unpaid`/`incomplete` (fixed by B2), the billing portal
  endpoint exists, `stripe` is upper-bounded at `<16.0`.

## In progress

- Nothing in flight. Everything this week is merged and on staging.
- Two follow-ups logged for next week, both surfaced by verifying rather than by building:
  the `unknown_subscription` incident firing on every checkout, and the 403 checkout copy.

## Slipped or blocked

- **CLEARED this session, but it had been the top of `STATE.md` since 2026-08-17 — 17
  days.** ~20 minutes of clicking, on the critical path, blocking the billing click-through
  and therefore Phase B's close. This is the week's real lesson: the process artifacts stayed
  perfectly current *about being blocked* while the critical path did not move. The tell is
  that the next action was a human-only step no agent could pick up.
  - Countermeasure adopted: when the top of `STATE.md` names a human-only step, that step is
    the only thing that happens next session. No new feature work in front of it.
- The Phase A residual staging click-through is still outstanding (needs a real inbox).

## Decisions made (link ADRs)

- **[ADR-0027](../../adr/0027-playwright-e2e-harness.md) — the e2e harness exists, is scoped
  to identity, and mirrors the production subdomain topology.** Amends ADR-0021 clause 3 and
  ADR-0026's D3 table. Blocking from day one with a deliberately small suite, because a
  non-blocking gate is one nobody fixes; retries are 1 in CI and a retry-pass is a finding,
  because retries as a flakiness cure turn an intermittent real bug green.

- **[ADR-0026](../../adr/0026-coverage-ratchet-not-aspirational-100.md) — coverage is a
  ratchet floor CI enforces, not an aspirational 100%.** Partially supersedes ADR-0021's
  clause 1. The floor may only go up; **lowering it requires an ADR**. 100% stays the target.
  ADR-0021's e2e clause is explicitly marked **not yet in force** — no Playwright harness
  exists anywhere — and `CLAUDE.md` D3/D9 now say so.
  - The honest framing: this **lowers the stated standard** (100 → 92/97). It trades a number
    nobody enforced for a smaller one everybody does. Recorded as a retreat, not as progress.
  - Root cause worth remembering: the gate stayed off for three months because turning it on
    would have failed every unrelated PR, so the honest answer was always "not this PR". A
    gate whose entire adoption cost lands on whoever touches it next never gets adopted.
    Setting the floor to *what already passes* makes adoption free.

## Next week's focus

1. Close both billing specs — a decision. The only unverified claim is the `past_due`
   renewal path, which needs Stripe **test clocks**: it cannot be clicked, and it is the
   transition that actually removes entitlement.
2. Wire *some* security/perf scanning into CI (`pip-audit` + `gitleaks` at minimum). It is
   the last big documented-but-not-running gate.
3. Playwright harness as its own D1 slice — it is the last wholly-unmet ADR-0021 clause.
4. Ratchet the floors: `platform/management/commands/seed.py` is 0%, and the dashboard's
   `routes/__root.tsx` / `_authed.tsx` / `register.tsx` shells and
   `features/scheduling/queries.ts` are 0%.

## D7 Friday review — 2026-09-04

Closed this week: the coverage gates, the CI-on-PR gap, the billing period-end race, the
two verification follow-ups, the Playwright harness, trunk-based flow, and enforced API
versioning. Six ADRs' worth of "the rule was written down but never ran" turned into rules
that run.

**Found during the handoff gate, and it is the same shape again:** no automated security or
performance scanning exists anywhere — `semgrep`, `gitleaks` CLI, `pip-audit`, `trivy`,
`lhci` all absent. Every `/handoff` has been satisfied by "review manually", so no OWASP
pass, dependency-CVE scan or Core Web Vitals budget has ever executed. Logged under *blocks
launch*. Noting it rather than fixing it tonight is deliberate (D10, D11), but it is now the
oldest un-run gate in the project.

## Reflection

The week's through-line: every gate that was *documented but not executing* was hiding
something real. CI not running on `develop` hid that Phase B had never been checked before it
shipped. The dashboard's absent coverage tooling hid a 92% number nobody had seen. And the
click-through — the substitute for the e2e harness that does not exist — found a race that
479 backend tests, 363 dashboard tests and 100% branch coverage on `billing` all missed,
because it only exists between two real concurrent webhook deliveries.

The lesson is not "write more tests". It is that a check which does not run is worse than no
check, because it also spends the credibility of the checks that do.

## Energy check (1-5)

4 — process cleanup and one well-scoped bug fix rather than feature grind. No D11 concern.

## Later the same day — 2026-09-04, second session

Cleared **both** remaining items from "Next week's focus" #1 and the e2e follow-on, in one
sitting: e2e extended past identity (6 flows → 20 — availability, family, account+email,
app shell; backend #38, dashboard #32, meta #140), then both billing specs closed
(meta #141). Four PRs, all green, two verified staging deploys.

Two things worth carrying forward.

**The conversion paid for itself immediately, which is the argument for the harness.** Every
new flow is a *write*, so the suite finally exercises CSRF on a mutation across the ADR-0019
boundary — the original six were reads plus one login. It found two defects vitest cannot
reach: the mobile drawer does not restore focus when it closes (measured, not assumed —
`document.activeElement` is `<body>`; it opens from `AppShell` state rather than a
`Dialog.Trigger`, so Radix has nothing to restore to), and two `/family` buttons share the
accessible name "Add child" — exactly the class of finding role-plus-name selectors surface
and a `data-testid` would hide. Both logged, neither fixed (D10). Also: the spec budgeted
~8m of CI for the login-per-spec cost; it came in at 2m40s. Wrong by a wide margin, and
recorded as wrong rather than quietly dropped.

**Closing B1 found that it contradicted the shipped behaviour.** It stated
`past_due ⇒ not entitled` in four places; B2's R8 had reversed that eight weeks earlier.
Struck through with a pointer to R8, not deleted — a closed spec is a record of what was
decided and when, and rewriting it would hide that the entitlement rule changed mid-phase.
The rule adopted: **closing a spec asserts its scope was built and verified; it does not
assert the phase is done.** An open risk belongs in `ISSUES.md`, not in a spec held at
`draft` — holding B1 open as a proxy for the `past_due` risk is precisely what let it drift
out of sync with the code it described. Same failure mode as this week's other lesson: an
artifact that stays *formally* current while quietly ceasing to be *true*.

Phase B now has exactly one gate: `past_due` via Stripe test clocks. Scoped precisely in
`ISSUES.md` — the state machine is covered at the provider seam; what is untested is the
wiring from real Stripe's renewal to those handlers. It needs a spec, and the spec needs one
human decision: CI harness, or manual runbook procedure?

Energy: 4. Long session, but process-clearing rather than grind.

## Later still — 2026-09-04, third session

Closed Phase B. The `past_due` → `unpaid` renewal path — the last gate, and the only one
that could not be reached by clicking — now runs nightly against real Stripe via test
clocks (spec + plan written this session; backend #39, meta #145 + #146).

**The harness earned its keep before it finished shipping.** Three findings, and only one
was in the harness itself. The Stripe account was set to *cancel* after failed retries
rather than *mark unpaid*, which meant `unpaid` was unreachable in production too — leaving
that branch of `_is_paid_through`, the `unique_live_subscription_per_user` unpaid clause,
and the dashboard's unpaid copy as dead code nobody had noticed. The final review then
caught the probe tests leaking a Stripe customer per run (~730/year on the shared account),
and a workflow env var that would have made the nightly *permanently* red in a way that
reads as a broken harness rather than broken config: an unset GitHub secret resolves to the
empty string, so `os.environ.get(name, default)` never reaches its default.

**Three things in my own approved spec turned out to be wrong**, all found by reading code
rather than by running it, and all corrected in the spec rather than worked around. The
harness could not live where the spec put it — that package's conftest has an `autouse`
fixture swapping in `FakePaymentProvider`, so it would have passed while touching no Stripe
at all. The API-version assertion could not work as described, because `stripe listen`
forwards at the account default and cannot be told otherwise. And it could not drive
checkout, because kaleem only creates a `Subscription` from `checkout.session.completed`.
The lesson is the same one as B1 drifting out of sync with its code: a spec is a hypothesis
until something executes it.

**The mutation check is the result that matters.** Breaking `_apply_invoice_payment_failed`
to write `ACTIVE` turns the harness red. Without that, this would be one more documented
gate that never actually ran — the exact shape of the coverage and e2e gates before
ADR-0026/0027, and the failure mode this project keeps rediscovering.

Also worth recording: the first real workflow run **failed**, on a secret name my spec and
my plan disagreed about (`STRIPE_SECRET_KEY` vs `STRIPE_TEST_SECRET_KEY`). The pre-flight
scan compared task-to-task interfaces but never compared the plan against the spec. Running
it deliberately rather than waiting for 03:17 is the only reason that was caught awake.

Energy: 3. Long session, heavy on adjudication rather than typing. Watch D11.

## 2026-09-04 (later) — the security gate runs, and it found something real

ADR-0030 wires `gitleaks` and `pip-audit` into CI as blocking checks and Lighthouse into a
nightly workflow against staging. This was the oldest un-run gate in the project: the
Definition of Done has asked for a security pass and a performance budget since
2026-06-17, and until today the answer was a human saying "reviewed manually" at handoff.
Third time this project has found the same shape — coverage (ADR-0026), e2e (ADR-0027),
now this.

**The first run paid for itself in one command.** `gitleaks` over 439 commits returned 13
findings. Nine are the old MVP's `.envs/` committed wholesale in April: an `sk_live_`
Stripe secret key, an AWS `AKIA` access key *and* its secret, the Django `SECRET_KEY`,
Postgres and Flower passwords. Deleted from the tree, kept by history, on GitHub. Rotation
is now next action #1 — a scanner cannot un-leak anything. Four more were a stray
`portal-snapshot.md` at the repo root: a Playwright accessibility dump from yesterday's
billing click-through, carrying a Stripe test-mode portal session secret. Deleted.

**Allowlisted by fingerprint, not by rule.** `.gitleaksignore` carries one annotated line
per historical finding, so a new secret in a new commit still fails while the known
exposure stays legible. Disabling the `generic-api-key` rule would have been one line and
would have blinded the gate; rewriting history would have broken every SHA quoted in
`docs/` and every submodule pointer, and would still not have revoked a single key.

**Everything was validated locally before the PR** — gitleaks green with the ignore file
and red without it, `pip-audit` clean on the production dependency set in an isolated
venv, `lhci autorun` green against real staging over 3 runs × 2 URLs. Setting a threshold
without measuring first is how you ship a gate that is red on day one and muted by day
three.

**pip-audit is scoped to production dependencies only.** A CVE in `pytest` cannot reach a
user, and a gate that goes red for something harmless teaches people to click past it.
Same reasoning as the e2e table: narrow and honest beats broad and ignored.

Lighthouse floors are ratchets set to measured values (ADR-0026's sense), and the four
audits that keep the categories off 100 — no meta description on marketing, contrast and
console errors and an invalid `robots.txt` on the dashboard — went to `ISSUES.md` rather
than being fixed here (D10).

Energy: 3.

## 2026-09-04 (later) — the drawer focus defect, fixed and now asserted

Next action #2 off `STATE.md`: the mobile nav drawer never restored focus. Dismiss it with
Escape and a keyboard user landed on `<body>`, at the top of the document, every time
(WCAG 2.4.3). Measured yesterday while writing `e2e/shell.spec.ts`, logged to `ISSUES.md`
rather than fixed there (D10) — this is that slice.

Cause is the one the issue named: the drawer opens from `AppShell` state, not a
`Dialog.Trigger`, so Radix has no trigger to hand focus back to. `AppShell` now holds a ref
to the topbar's menu button and names it in `onCloseAutoFocus`. The `Dialog.Trigger`
alternative reaches the same place but couples `AppTopbar` to the drawer, so it lost.

**The e2e spec stopped documenting the gap and started asserting it.** Its old comment
explained, at length, why focus restore was *not* asserted. That comment is now a
`toBeFocused()` — the shape ADR-0027 exists for.

**Mutation-checked in a real browser, not just jsdom.** With the fix reverted and the
bundle rebuilt, the new e2e assertion goes red; with it, green. That is the check that
separates a test which passes from a test which *tests*. Ran the full 20-flow suite against
a real stack locally (Django on `api.kaleem.localhost:8000`, the built dashboard on
`app.kaleem.localhost:4173`) — 20 passed.

Unit coverage moved to 92.92 / 87.9 / 84.51, still above the floors; not raising the
ratchet for a delta this small.

Energy: 3.
